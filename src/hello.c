//primeiro arquivo em linguagem C
//vamos importar a bibloteca base para executar o nosso progama
//a biblioteca se chama stdio(std->standard | i -> input | 0 -> output)
//para de importar, usaremos o comandos #incluide
#include <stdio.h>

//abaixo iremos escrever os comandos que serao realmente executados
//utilizaremos a funcao main(principal) para iniciar a execucao
int main(){

    // o comando print exibir uma mensagem em tela de controle
printf("hello, world!");

//  informa ao compilador(tradutor) gcc que o progama acabou
return 0;

}

    





